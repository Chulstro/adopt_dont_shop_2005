<h1><%= @pet.name %></h1>

<img src= <%= @pet.image %>>
<p><%= @pet.description %></p>
<p><%= @pet.approx_age %></p>
<p><%= @pet.sex %></p>

<% if @pet.adoptability == true%>
  <p>Adoption is a go!</p>
<% else %>
  <p>Adoption is Pending</p>
<% end %>

<%= link_to "Update Pet", "/pets/#{@pet.id}/edit" %>

<%= link_to "Delete Pet", "/pets/#{@pet.id}", method: :delete %>
